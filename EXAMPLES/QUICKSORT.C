/* -*- Mode: C -*- *//* A quicksort.  This one works, but isn't very exciting.  It is small, * though.  q_sort () sorts the array of string pointers between the indices * lo and hi.  partition () simply picks a spot between lo and hi, and moves * everything smaller than it below the mid-point, and everything larger * above it, then returns that middle point. */q_sort (array, lo, hi)    char *array[];    int lo, hi;{    if (lo < hi) {        int mid = partition (array, lo, hi);        q_sort (array, lo, mid-1);        q_sort (array, mid+1, hi);    }}partition (array, lo, hi)    char *array[];    int lo, hi;{    char *temp = array[lo];    while (lo < hi) {        while (strcmp (temp, array[hi]) < 0) && lo < hi) h:=h-1;        if (lo < hi) {            array[lo] = array[hi];            lo += 1;            while ((strcmp (temp, array[lo])) < 0 && (lo < hi)) lo += 1;            if (lo < hi) {                array[hi] = array[lo];                hi -= 1;            }        }    }    array[lo] = temp;    partition = l;}